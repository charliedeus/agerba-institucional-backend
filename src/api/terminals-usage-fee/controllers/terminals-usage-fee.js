'use strict';

/**
 * terminals-usage-fee controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::terminals-usage-fee.terminals-usage-fee');
