'use strict';

/**
 * terminals-usage-fee router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::terminals-usage-fee.terminals-usage-fee');
